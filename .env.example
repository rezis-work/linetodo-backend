# Database Connection Strings
# DATABASE_URL is the pooled connection string (recommended for runtime)
# Use this for your application runtime connections
DATABASE_URL="postgresql://user:password@host:5432/database?sslmode=require"

# DATABASE_URL_DIRECT is the direct connection string (optional, for migrations)
# Use this for migrations if you encounter connection pool issues
# If not provided, DATABASE_URL will be used for migrations
# DATABASE_URL_DIRECT="postgresql://user:password@host:5432/database?sslmode=require"

# Server
PORT=3000
NODE_ENV=development

# Test Database (optional - for running database tests)
# If not provided, tests will use a default test database URL
# TEST_DATABASE_URL="postgresql://user:password@host:5432/test_db?sslmode=require"
